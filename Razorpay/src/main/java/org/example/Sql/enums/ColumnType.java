package org.example.Sql.enums;

/**
 * Enum representing supported column data types in the database.
 * Following the Open-Closed Principle - new types can be added without modifying existing code.
 */
public enum ColumnType {
    /**
     * Integer type column
     */
    INT,
    
    /**
     * String type column
     */
    STRING
}





